.vs/
__pycache__/
*.pyc
node_modules/
playwright-report/
test-results/
# Vite build output. Generated by `npm run build`, never edited by hand, and not the
# published artifact on this branch — `netlify.toml` still publishes `public/` here.
# Tracking it means every build dirties the tree and conflicts with feat/vite-migration,
# which ignores it. Deploy from a build, not from committed output.
dist/
